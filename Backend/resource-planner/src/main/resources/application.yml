server:
  port: 8090
spring:
  application:
    name: ms-resource-planner
  datasource:
    url: jdbc:postgresql://localhost:5432/db_planner
    username: user_vrakben
    password: password_vrakben
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/