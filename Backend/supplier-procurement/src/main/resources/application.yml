server:
  port: 8092
spring:
  application:
    name: ms-supplier-procurement
  datasource:
    url: jdbc:postgresql://localhost:5432/db_procurement
    username: user_vrakben
    password: password_vrakben
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/