server:
  port: 8089

spring:
  application:
    name: ms-vehicle-history
  datasource:
    url: jdbc:postgresql://localhost:5432/db_history
    username: user_vrakben
    password: password_vrakben
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/