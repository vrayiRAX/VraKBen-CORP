server:
  port: 8093
spring:
  application:
    name: ms-warehouse-location
  datasource:
    url: jdbc:postgresql://localhost:5432/db_warehouse
    username: user_vrakben
    password: password_vrakben
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/