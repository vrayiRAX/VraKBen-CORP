server:
  port: 8085
spring:
  application:
    name: ms-customer-profile
  datasource:
    url: jdbc:postgresql://localhost:5432/db_customers
    username: user_vrakben
    password: password_vrakben
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/