server:
  port: 8086
spring:
  application:
    name: ms-job-orders
  datasource:
    url: jdbc:postgresql://localhost:5432/db_job_orders
    username: user_vrakben
    password: password_vrakben
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/